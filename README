-----------
Notes
___________

To parse http.log as generated by bro

Tries to find http.log for a sample by looking in a directory that has logs given after processed by bro

If not found finds the corresponding pcap in directory and performs broify.sh on it. There will be a directory with the md5 as the name in the current directory.